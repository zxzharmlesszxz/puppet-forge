global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/puppet-forge.yml

scrape_configs:
  - job_name: puppet-forge
    static_configs:
      - targets:
          - app:9090
