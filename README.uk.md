# Сервіс Puppet Forge

> **Операційна карта:** [SCHEMA.uk.md](SCHEMA.uk.md) описує роботу одного й кількох
> екземплярів, межі спільного стану, запобіжники та точкові перевірки кожного
> важливого вузла.

Сервіс на Go для внутрішнього аналога Puppet Forge:

- публікація модулів і версій;
- керування просторами публікації команд;
- доступ команд за токенами та через OIDC;
- проксіювання та кешування офіційного API Puppet Forge;
- зберігання артефактів у GCS або S3-сумісному сховищі;
- зберігання метаданих у SQL-сховищі;
- запуск у Kubernetes.

## Можливості

Поточна реалізація покриває:

- `POST /api/v1/modules` для завантаження нової версії модуля;
- `GET /api/v1/manage/publish-spaces` для списку просторів, доступних автентифікованому суб'єкту публікації;
- `GET /api/v1/modules?limit=20&offset=0` для списку модулів; відповідь містить `items`, `limit`, `offset` і `total`;
- `GET /api/v1/modules/{owner}/{name}` для картки модуля;
- `DELETE /api/v1/modules/{owner}/{name}` для видалення модуля з правами глобального адміністратора або адміністратора команди;
- `GET /api/v1/modules/{owner}/{name}/versions/{version}` для конкретного релізу;
- `DELETE /api/v1/modules/{owner}/{name}/versions/{version}` для видалення окремої версії з правами глобального адміністратора або адміністратора команди;
- `GET /api/v1/modules/{owner}/{name}/versions/{version}/download` для завантаження збереженого артефакту через сервіс;
- `GET /` для початкової HTML-сторінки зі списком локально опублікованих модулів;
- `GET /modules/{owner}/{name}` для HTML-сторінки модуля з README у Markdown, вибором версії та інструкціями встановлення;
- `GET /v3/*` і `HEAD /v3/*` для зворотного проксіювання на офіційний API Puppet Forge;
- `/manage` для огляду доступних модулів і команд без дій керування;
- `/manage/modules` для публікації, видалення, пошуку та імпорту модулів із зовнішнього Forge;
- `/manage/teams` для списку доступних команд та окремі сторінки `/access`, `/tokens` і `/modules` для керування вибраною командою;
- `/manage/admin/access` і `/manage/admin/spaces` для структурованої глобальної конфігурації доступу, що зберігається у БД;
- `ADMIN_TOKEN` для початкового та аварійного доступу;
- вхід через OIDC для глобальних адміністраторів, адміністраторів команд і командних груп публікації;
- `PUBLIC_MODULE_ACCESS` для режиму публічного машинозчитуваного доступу та встановлення;
- відстеження активних релізів, яке забороняє видалення останніх і використовуваних версій та приховує недоступні дії видалення у `/manage/modules`;
- `GET /healthz` і `GET /readyz`.

Поза поточною реалізацією:

- пошук із фільтрами, рейтингами, залежностями й перевіреними видавцями;
- повна API-сумісність з офіційним Puppet Forge;
- CDN і підписані URL з TTL.

## Архітектура

- HTTP API: стандартний `net/http`;
- Рівень сервісу: валідація, оркестрація завантаження та запису метаданих;
- сховище артефактів: GCS або S3-сумісне об'єктне сховище;
- сховище метаданих: `postgres://` або `sqlite://`, яке обирається через `DATABASE_DSN`;
- зовнішній проксі: зворотний проксі на офіційний API Forge з TTL-кешем у пам'яті для JSON-відповідей на GET/HEAD.

Потік публікації:

1. Клієнт надсилає багатокомпонентний запит із вибраним `space` та артефактом tar.gz.
2. Сервіс перевіряє право автентифікованого суб'єкта публікувати в цей простір.
3. Ідентичність і метадані читаються з архіву, а простір імен має збігатися з вибраним простором.
4. MD5, SHA-256 і розмір обчислюються один раз. Архів створюється без перезапису за шляхом `modules/<owner>/<name>/<version>/<sha256>.tar.gz`.
5. Ідентичність локального релізу незмінна: повторна публікація тих самих байтів є ідемпотентною, а інший вміст для наявної пари модуль/версія повертає `409 Conflict`.
6. Метадані релізу створюються в SQL-сховищі й повертаються клієнту. За однозначної помилки збереження незакомічений об'єкт видаляється компенсаційною операцією.

Потік проксіювання:

1. Клієнт викликає маршрут `/v3/...` нашого сервісу.
2. Сервіс надсилає запит на офіційний API Puppet Forge.
3. JSON-відповіді кешуються в пам'яті на `UPSTREAM_PROXY_JSON_CACHE_TTL`.
4. Перший запит файлу за маршрутом `/v3/files/...` запускає його потоковий запис у create-only object під префіксом `upstream-cache/`, після чого сервіс так само потоково віддає завершений shared object без перенаправлення. Повний tar-архів не зберігається в пам'яті proxy.

## Конфігурація

Див. [`.env.example`](.env.example).

## Локальний старт

```bash
go test ./...
go run ./cmd/server
```

Тести відповідності PostgreSQL запускаються окремо, щоб звичайний `go test ./...` не залежав від локального Docker/PostgreSQL. Для перевірки однакової поведінки SQLite і PostgreSQL задай DSN тестової бази:

```bash
make test-postgres
```

Інтеграційні тести об'єктних сховищ також запускаються окремо. Коли MinIO та fake-gcs-server з локального набору Compose запущені, виконай:

```bash
make test-object-storage
```

Об'єднаний target запускає контракти S3 та GCS. Вони перевіряють незмінні create-only завантаження, потокове повне й діапазонне читання, отримання списку та ідемпотентне видалення; контракт S3 додатково перевіряє конкурентний запис одного ключа й очищення після скасування. Для одного backend використовуй `make test-s3-storage` або `make test-gcs-storage`; адреси інших ізольованих емуляторів передаються через відповідні змінні `PUPPET_FORGE_TEST_*_ENDPOINT`.

## Як опублікувати свій модуль

Сервіс приймає `tar.gz` архів Puppet-модуля через `POST /api/v1/modules` як `multipart/form-data`.

Мінімальні вимоги до модуля:

- в архіві має бути рівно один валідний `metadata.json`;
- усі записи архіву мають бути всередині одного канонічного кореневого каталогу `<owner>-<name>-<version>`;
- `metadata.json` і необов'язковий README мають бути безпосередніми дочірніми файлами кореневого каталогу;
- абсолютні шляхи, вихід до батьківських каталогів, посилання, пристрої та інші спеціальні записи tar відхиляються;
- розмір окремого запису й загальний розпакований розмір архіву обмежені;
- `metadata.json.name` має бути у форматі `<owner>-<name>`, наприклад `teamname-apache`;
- `owner` може містити латинські літери в обох регістрах і цифри; коротке ім'я модуля `name` може містити малі латинські літери, цифри та символи підкреслення; дефіс зарезервовано як однозначний розділювач цих частин;
- у `metadata.json.version` має бути версія;
- простір імен із `metadata.json.name` має збігатися з вибраним `space`;
- README бажано покласти в `README.md`, тоді він з'явиться на HTML-сторінці модуля.

Приклад структури:

```text
teamname-apache-1.2.3/
  metadata.json
  README.md
  manifests/
    init.pp
```

Приклад `metadata.json`:

```json
{
  "name": "teamname-apache",
  "version": "1.2.3",
  "summary": "Модуль Apache",
  "author": "Приклад команди"
}
```

Зібрати архів можна стандартним `puppet module build`:

```bash
cd /path/to/teamname-apache
puppet module build
```

Після цього Puppet зазвичай покладе артефакт у `pkg/teamname-apache-1.2.3.tar.gz`.

Для публікації передаються тільки цільовий простір та архів.

```bash
export FORGE_URL="https://forge.example.com"
export PUBLISH_TOKEN="replace-me"

curl -X POST "${FORGE_URL}/api/v1/modules" \
  -H "Authorization: Bearer ${PUBLISH_TOKEN}" \
  -F "space=teamname" \
  -F "file=@pkg/teamname-apache-1.2.3.tar.gz"
```

Важливо:

- для публікації потрібні тільки поля `space` і `file`;
- власник, назва, версія, короткий і повний описи та інші метадані беруться виключно з `metadata.json`;
- задані вручну поля ідентичності або перевизначення метаданих відхиляються;
- простір імен архіву має збігатися з вибраним простором;
- токен має мати роль `publish` і давати право на потрібний простір публікації. У структурованому інтерфейсі простір із назвою `Team` додається автоматично; додаткові простори призначають глобальні адміністратори у `/manage/admin/spaces`;
- багатокомпонентне завантаження, обчислення контрольних сум, запис до об'єктного сховища й завантаження релізу працюють потоково; великий архів не копіюється в один буфер у пам'яті.

Переглянути простори, доступні токену публікації:

```bash
curl "${FORGE_URL}/api/v1/manage/publish-spaces" \
  -H "Authorization: Bearer ${PUBLISH_TOKEN}"
```

Швидка перевірка після публікації:

```bash
curl "${FORGE_URL}/api/v1/modules/teamname/apache"
curl -I "${FORGE_URL}/api/v1/modules/teamname/apache/versions/1.2.3/download"
```

HTML-сторінка модуля буде доступна за адресою:

```text
https://forge.example.com/modules/teamname/apache
```

Вебінтерфейс також містить:

- інтерактивний фільтр на початковій сторінці `/`;
- відтворення README у форматі Markdown на сторінці модуля;
- випадний список вибору версії модуля;
- готові інструкції встановлення модуля й підключення цього сервісу як власного Forge.

Спостережуваність:

- `puppet_forge_build_info{version,go_version}` містить версію сервісу та Go runtime;
- окремий сервер метрик задається через `METRICS_ADDR` і не маршрутизується через Ingress застосунку;
- журналювання всіх HTTP-запитів;
- метрики Prometheus на `GET /metrics`.
- Інвентарні метрики для модулів:
  - `puppet_forge_modules{owner}`
  - `puppet_forge_module_releases{source}`
  - `puppet_forge_module_latest_releases{source}`
  - `puppet_forge_module_metrics_ready`
  - `puppet_forge_module_metrics_last_success_timestamp_seconds`
  - `puppet_forge_module_metrics_refresh_errors_total`
  - `puppet_forge_module_metrics_owners_total`
  - `puppet_forge_module_metrics_truncated`
- Операційні метрики:
  - `puppet_forge_publish_total{result}`
  - `puppet_forge_delete_total{result,kind}`
  - `puppet_forge_release_usage_mark_total{result}`
  - `puppet_forge_artifact_stream_total{source,result}`
  - `puppet_forge_artifact_stream_bytes_total{source}`
  - `puppet_forge_upstream_sync_total{result,trigger}`
  - `puppet_forge_upstream_refresh_cycles_total{result}`
  - `puppet_forge_upstream_refresh_duration_seconds`
  - `puppet_forge_upstream_refresh_last_duration_seconds`
  - `puppet_forge_upstream_refresh_last_timestamp_seconds`
  - `puppet_forge_upstream_refresh_last_success_timestamp_seconds`
  - `puppet_forge_upstream_refresh_last_error_timestamp_seconds`
  - `puppet_forge_upstream_refresh_modules{result}`
  - `puppet_forge_upstream_cache_requests_total{kind,result}`

Метрики:

- `puppet_forge_modules` експортує кількість модулів, згруповану за обмеженою міткою `owner`;
- `puppet_forge_module_releases` експортує кількість локально доступних релізів, згруповану за міткою `source`;
- `puppet_forge_module_latest_releases` експортує кількість останніх релізів, згруповану за міткою `source`;
- інвентарні метрики рівня релізів навмисно агреговані, щоб `/metrics` залишався швидким навіть за великої кількості версій модулів.

Назви й версії окремих модулів навмисно не експортуються як мітки Prometheus. Для інвентаризації на рівні релізів використовуй API каталогу або вебінтерфейс керування.

Повний опис метрик див. у [METRICS.uk.md](METRICS.uk.md).

Параметри запуску можна передавати через змінні середовища або аргументи командного рядка. Якщо задано обидва варіанти, аргументи командного рядка мають пріоритет.

Бінарний файл виводить версію збірки й завершує роботу за командою:

```bash
puppet-forge --version
```

Назви аргументів утворюються з назв змінних середовища: використовуються малі літери, а символи підкреслення замінюються дефісами:

```text
DATABASE_DSN -> --database-dsn
LOG_LEVEL -> --log-level
MANAGE_SESSION_SECRET -> --manage-session-secret
ACCESS_TOKEN_PEPPER -> --access-token-pepper
TRUSTED_PROXY_CIDRS -> --trusted-proxy-cidrs
TRUST_FORWARDED_HEADERS -> --trust-forwarded-headers
ALLOWED_PUBLIC_HOSTS -> --allowed-public-hosts
PUBLIC_MODULE_ACCESS -> --public-module-access
OIDC_SCOPES -> --oidc-scopes
OIDC_SIGNING_ALGORITHMS -> --oidc-signing-algorithms
UPSTREAM_PROXY_JSON_CACHE_TTL -> --upstream-proxy-json-cache-ttl
UPSTREAM_PROXY_JSON_STALE_TTL -> --upstream-proxy-json-stale-ttl
```

Приклад:

```bash
puppet-forge \
  --database-dsn "postgres://forge:forge@postgres:5432/forge?sslmode=disable" \
  --admin-token "replace-me-bootstrap-token" \
  --artifact-backend s3 \
  --artifact-endpoint "http://minio:9000" \
  --artifact-bucket "puppet-forge-artifacts" \
  --public-module-access=false
```

Змінні середовища:

| Змінна                          | За замовчуванням                  | Обов'язкова                  | Опис                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------- | --------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `APP_ENV`                       | `dev`                             | ні                           | Назва середовища виконання для журналів і діагностики.                                                                                                                                                                                                                                                                                                                       |
| `LOG_LEVEL`                     | `info`                            | ні                           | Мінімальний рівень структурованого журналювання: `debug`, `info`, `warn` або `error`. На рівні `debug` сервіс журналює початок HTTP-запитів, діагностику авторизації, сесій і CSRF, етапи OIDC, звернення та кеш зовнішнього Forge, а також оренду фонового оновлення, не виводячи облікових даних або секретів сесій. Відповідний аргумент командного рядка: `--log-level`. |
| `HTTP_ADDR`                     | `:8080`                           | ні                           | Адреса, на якій HTTP-сервер слухає запити всередині контейнера або процесу.                                                                                                                                                                                                                                                                                                  |
| `METRICS_ADDR`                  | `:9090`                           | ні                           | Адреса окремого сервера метрик Prometheus. Має відрізнятися від `HTTP_ADDR`; мережевий доступ слід дозволити лише Prometheus.                                                                                                                                                                                                                                                |
| `READ_HEADER_TIMEOUT`           | `10s`                             | ні                           | Максимальний час читання заголовків HTTP. Значення має бути більше нуля.                                                                                                                                                                                                                                                                                                     |
| `READ_TIMEOUT`                  | `0s`                              | ні                           | Максимальний час читання всього HTTP-запиту. Типове значення вимикає загальний термін, щоб великі або повільні завантаження модулів могли передаватися потоком; обмеження заголовків і розміру тіла лишаються активними. Додатне значення варто задавати лише тоді, коли середовище гарантує достатню швидкість завантаження.                                                |
| `WRITE_TIMEOUT`                 | `0s`                              | ні                           | Максимальний час запису всієї HTTP-відповіді. Типове значення вимикає загальний термін, щоб великі або повільні завантаження артефактів могли передаватися потоком; за потреби обмеження передачі клієнтам треба налаштувати на Ingress.                                                                                                                                     |
| `IDLE_TIMEOUT`                  | `60s`                             | ні                           | Максимальний час бездіяльності keep-alive між HTTP-запитами. Значення має бути більше нуля.                                                                                                                                                                                                                                                                                  |
| `HTTP_MAX_HEADER_BYTES`         | `1048576`                         | ні                           | Максимальний розмір заголовків запиту в байтах; дозволений діапазон від 4096 до 16777216.                                                                                                                                                                                                                                                                                    |
| `SHUTDOWN_TIMEOUT`              | `10s`                             | ні                           | Час на коректне завершення роботи.                                                                                                                                                                                                                                                                                                                                           |
| `DATABASE_BACKEND`              | визначається з `DATABASE_DSN`     | ні                           | Сховище метаданих: `postgres` або `sqlite`. У Helm його треба задавати явно, щоб перевірка топології не залежала від читання вмісту Secret.                                                                                                                                                                                                                                  |
| `DATABASE_DSN`                  | порожньо                          | так                          | DSN сховища метаданих. `postgres://...` і `postgresql://...` вмикають PostgreSQL, а `sqlite:///path/file.db` — SQLite.                                                                                                                                                                                                                                                         |
| `DATABASE_MAX_CONNS`            | `10`                              | ні                           | Максимальна кількість з'єднань PostgreSQL на одну репліку. Узгоджуй її з максимумом HPA, щоб сумарна кількість з'єднань не перевищувала ліміт бази.                                                                                                                                                                                                                          |
| `DATABASE_MIN_CONNS`            | `0`                               | ні                           | Мінімальна кількість з'єднань PostgreSQL, яку підтримує кожна репліка.                                                                                                                                                                                                                                                                                                       |
| `DATABASE_MAX_CONN_LIFETIME`    | `1h`                              | ні                           | Максимальний час життя з'єднання PostgreSQL у пулі; нуль вимикає завершення за віком.                                                                                                                                                                                                                                                                                        |
| `DATABASE_MAX_CONN_IDLE_TIME`   | `30m`                             | ні                           | Максимальний час простою з'єднання PostgreSQL у пулі; нуль вимикає завершення за простоєм.                                                                                                                                                                                                                                                                                   |
| `ADMIN_TOKEN`                   | порожньо                          | для порожньої БД             | Токен адміністратора для початкового або аварійного доступу. Не зберігається в БД; потрібен, щоб після чистого старту налаштувати `/manage/teams` і `/manage/admin/access`.                                                                                                                                                                                                  |
| `ACCESS_TOKEN_PEPPER`           | порожньо                          | так                          | Спільний секрет щонайменше на 32 байти для HMAC-SHA256-хешування токенів читання та публікації в БД. Має бути однаковим на всіх репліках і входити до процедур резервного копіювання та відновлення; його втрата робить збережені токени недійсними.                                                                                                                         |
| `ACCESS_TOKEN_HISTORY_TTL`      | `2160h`                           | ні                           | Строк зберігання метаданих прострочених і відкликаних токенів читання та публікації. Очищення асинхронно запускається під міжрепліковою SQL-lease одразу після старту й щодня; `0` зберігає неактивну історію безстроково. Активні токени це очищення не видаляє.                                                                                                            |
| `DELETED_RELEASE_TTL`           | `2160h`                           | ні                           | Строк зберігання позначок видалених зовнішніх релізів. Очищення асинхронно запускається під міжрепліковою SQL-lease одразу після старту й щодня; після завершення строку синхронізація може відновити реліз. `0` зберігає позначки безстроково.                                                                                                                              |
| `MANAGE_SESSION_SECRET`         | порожньо                          | так                          | Окремий спільний секрет щонайменше на 32 байти для HMAC-хешування непрозорих ідентифікаторів серверних сесій `/manage`. Він має бути стабільним, однаковим на всіх репліках і відрізнятися від `ACCESS_TOKEN_PEPPER`; ротація кожного секрету скасовує лише відповідний клас облікових даних.                                                                                |
| `ARTIFACT_BACKEND`              | `gcs`                             | ні                           | Сховище tar-архівів: `gcs` або `s3`.                                                                                                                                                                                                                                                                                                                                         |
| `ARTIFACT_ENDPOINT`             | `https://storage.googleapis.com`  | для `s3`; опційно для `gcs`  | Кінцева точка об'єктного сховища. Для `gcs` можна вказати емулятор або власний хост; для `s3` це S3-сумісна кінцева точка, наприклад MinIO.                                                                                                                                                                                                                                  |
| `ARTIFACT_BUCKET`               | порожньо                          | так                          | Контейнер для архівів модулів і кешу зовнішніх артефактів.                                                                                                                                                                                                                                                                                                                   |
| `ARTIFACT_PROJECT`              | порожньо                          | коли ввімкнено створення GCS | Проєкт GCP, який використовується лише для створення відсутнього контейнера, коли `ARTIFACT_ENSURE_BUCKET=true`. Для звичайних операцій з об'єктами та `s3` не використовується.                                                                                                                                                                                             |
| `ARTIFACT_ENSURE_BUCKET`        | `false`                           | ні                           | Якщо `true`, під час запуску перевіряє наявність контейнера GCS і створює його за потреби. У production лишайте вимкненим, щоб runtime identity потребувала лише прав на об'єкти; вмикайте для локального емулятора або окремої привілейованої provisioning identity.                                                                                                        |
| `ARTIFACT_GCS_ANONYMOUS`        | `false`                           | ні                           | Вимикає автентифікацію GCS. Вмикайте лише для локального емулятора; власні production endpoints використовують application-default credentials, якщо цей параметр не задано явно.                                                                                                                                                                                            |
| `ARTIFACT_PREFIX`               | `modules`                         | ні                           | Префікс усередині контейнера для локально опублікованих модулів.                                                                                                                                                                                                                                                                                                             |
| `ARTIFACT_REGION`               | `us-east-1`                       | ні                           | Регіон для S3-сумісного клієнта.                                                                                                                                                                                                                                                                                                                                             |
| `ARTIFACT_ACCESS_KEY_ID`        | порожньо                          | для приватного `s3`          | Ідентифікатор ключа доступу до S3-сумісного сховища.                                                                                                                                                                                                                                                                                                                         |
| `ARTIFACT_SECRET_ACCESS_KEY`    | порожньо                          | для приватного `s3`          | Секретний ключ доступу до S3-сумісного сховища.                                                                                                                                                                                                                                                                                                                              |
| `ARTIFACT_PATH_STYLE`           | `true`                            | ні                           | Вмикає S3 URL зі шляховою адресацією. Корисно для MinIO, сумісності GCS і локальних кінцевих точок.                                                                                                                                                                                                                                                                          |
| `PUBLIC_BASE_URL`               | порожньо                          | ні                           | Необов'язкова резервна адреса для побудови абсолютних URL. Якщо значення порожнє, сервіс використовує перевірене ім'я вузла із запиту та, коли це явно дозволено, передані проксі ім'я вузла й протокол.                                                                                                                                                                     |
| `ALLOWED_PUBLIC_HOSTS`          | порожньо                          | ні                           | Необов'язковий список дозволених публічних імен вузлів через кому або пробіл, за потреби з портами. Невідоме ім'я отримує `421`; запис без порту дозволяє це ім'я на будь-якому порту.                                                                                                                                                                                       |
| `TRUSTED_PROXY_CIDRS`           | порожньо                          | ні                           | CIDR безпосередніх Ingress або зворотних проксі. Вони можуть передавати ідентичність клієнта, а з `TRUST_FORWARDED_HEADERS=true` також зовнішні ім'я вузла й протокол.                                                                                                                                                                                                       |
| `TRUST_FORWARDED_HEADERS`       | `false`                           | ні                           | Приймає перевірені `Forwarded` і `X-Forwarded-*`, лише коли безпосередній вузол належить до `TRUSTED_PROXY_CIDRS`. Інакше перед маршрутизацією всі передані проксі заголовки видаляються.                                                                                                                                                                                    |
| `PUBLIC_MODULE_ACCESS`          | `false`                           | ні                           | Якщо `true`, API читання, завантаження та `/v3/*` відкриті без токена. Якщо `false`, ці маршрути встановлення й API потребують токена читання, публікації або адміністратора. HTML-каталог лишається доступним; публікація, видалення й керування завжди закриті.                                                                                                            |
| `ACTIVE_RELEASE_TTL`            | `720h`                            | ні                           | Час, протягом якого реліз вважається активним після запиту r10k або `puppet module install`; активні й останні версії не можна видалити через API або `/manage/modules`.                                                                                                                                                                                                     |
| `SECURITY_HSTS_ENABLED`         | `false`                           | ні                           | Вмикає заголовок відповіді `Strict-Transport-Security`. Для локального HTTP залишай вимкненим; вмикай лише тоді, коли публічна кінцева точка завжди працює через HTTPS.                                                                                                                                                                                                      |
| `WEB_AUTH_MODE`                 | `none`                            | ні                           | Режим вебавтентифікації: `none` або `oidc`. Автентифікація API за токенами працює незалежно від цього.                                                                                                                                                                                                                                                                       |
| `OIDC_ISSUER_URL`               | порожньо                          | для `WEB_AUTH_MODE=oidc`     | URL виявлення видавця OIDC, наприклад URL постачальника застосунку Authentik.                                                                                                                                                                                                                                                                                                |
| `OIDC_CLIENT_ID`                | порожньо                          | для `WEB_AUTH_MODE=oidc`     | Ідентифікатор клієнта OIDC.                                                                                                                                                                                                                                                                                                                                                  |
| `OIDC_CLIENT_SECRET`            | порожньо                          | для `WEB_AUTH_MODE=oidc`     | Секрет клієнта OIDC.                                                                                                                                                                                                                                                                                                                                                         |
| `OIDC_REDIRECT_URL`             | порожньо                          | ні                           | Явний URL зворотного виклику. Якщо значення порожнє, адреса `/auth/callback` будується з базового URL поточного запиту; це рекомендований режим для кількох Ingress.                                                                                                                                                                                                         |
| `OIDC_LOGOUT_URL`               | автовиявлення/порожньо            | ні                           | URL завершення сесії постачальника. Якщо не задано, сервіс намагається отримати `end_session_endpoint` через виявлення OIDC.                                                                                                                                                                                                                                                 |
| `OIDC_COOKIE_SECRET`            | порожньо                          | для `WEB_AUTH_MODE=oidc`     | Секрет щонайменше з 32 байтів для шифрування тимчасової cookie стану та HMAC-хешування непрозорих ідентифікаторів OIDC-сесій у SQL. Має бути стабільним і однаковим на всіх репліках.                                                                                                                                                                                        |
| `OIDC_SCOPES`                   | `openid profile email`            | ні                           | Області OIDC, розділені пробілами. `openid` додається завжди; додай потрібну постачальнику область, наприклад `groups`, якщо вона потрібна для отримання груп.                                                                                                                                                                                                               |
| `OIDC_SIGNING_ALGORITHMS`       | `RS256`                           | ні                           | Розділений пробілами список дозволених асиметричних алгоритмів підпису ID-токена. Підтримуються `RS*`, `PS*`, `ES*` і `EdDSA`; симетричні алгоритми та `none` відхиляються.                                                                                                                                                                                                  |
| `UPSTREAM_URL`                  | `https://forgeapi.puppetlabs.com` | ні                           | URL зовнішнього API Puppet Forge, до якого проксі звертається по модулі, відсутні локально.                                                                                                                                                                                                                                                                                  |
| `UPSTREAM_PROXY_JSON_CACHE_TTL` | `5m`                              | ні                           | TTL кешу в пам'яті для JSON-відповідей GET/HEAD зовнішнього проксі (`/v3/modules/...`, `/v3/releases/...`). Не керує кешем tar-архівів в об'єктному сховищі. `0s` фактично вимикає кеш JSON-відповідей.                                                                                                                                                                      |
| `UPSTREAM_PROXY_JSON_STALE_TTL` | `1h`                              | ні                           | Максимальний час після завершення `UPSTREAM_PROXY_JSON_CACHE_TTL`, протягом якого проксі може віддати застарілий JSON-кеш, якщо зовнішній Forge повернув помилку або недоступний. `0s` вимикає цю резервну поведінку.                                                                                                                                                        |
| `FORGE_CACHE_MAX_BODY_BYTES`    | `1048576`                         | ні                           | Максимальний розмір тіла зовнішньої JSON-відповіді, яку можна зберегти в кеші проксі у пам'яті.                                                                                                                                                                                                                                                                              |
| `MODULE_UPLOAD_MAX_BYTES`       | `134217728`                       | ні                           | Максимальний розмір архіву Puppet-модуля в байтах. Multipart framing має окремий обмежений запас; завеликий архів повертає `413 Request Entity Too Large` і не зчитується повністю в пам'ять.                                                                                                                                                                                |
| `UPSTREAM_ARTIFACT_MAX_BYTES`   | `134217728`                       | ні                           | Максимальний розмір зовнішнього tar-архіву з `/v3/files/...`, який проксі дозволяє завантажити до кешу або обхідним шляхом без сховища. Завелика upstream-відповідь вважається помилковою відповіддю шлюзу й повертає `502 Bad Gateway`.                                                                                                                                     |
| `UPSTREAM_ARTIFACT_ORPHAN_TTL`  | `24h`                             | ні                           | Пільговий період перед тим, як обраний через lease worker видалить об'єкт без SQL-посилання з `upstream-cache/`. Об'єкти з чинними посиланнями зберігаються. Нуль вимикає очищення.                                                                                                                                                                                          |
| `UPSTREAM_SYNC_INTERVAL`        | `0s`                              | ні                           | Інтервал фонового оновлення вже закешованих зовнішніх модулів. `0s` вимикає фонове оновлення.                                                                                                                                                                                                                                                                                |
| `UPSTREAM_SYNC_LIMIT`           | `1000`                            | ні                           | Максимальна кількість зовнішніх модулів, яку обробляє один цикл оновлення.                                                                                                                                                                                                                                                                                                   |
| `UPSTREAM_SYNC_CONCURRENCY`     | `8`                               | ні                           | Максимальна кількість зовнішніх модулів, які репліка-лідер оновлює паралельно за один цикл.                                                                                                                                                                                                                                                                                  |
| `METRICS_MODULE_LIMIT`          | `10000`                           | ні                           | Максимальна кількість часових рядів із міткою `owner`, які інвентарні метрики експортують за один прохід збору.                                                                                                                                                                                                                                                              |
| `METRICS_REFRESH_INTERVAL`      | `30s`                             | ні                           | Базовий інтервал перебудови snapshot інвентарних метрик кожною реплікою зі спільної SQL-бази. Невеликий випадковий зсув не дає реплікам одночасно навантажувати SQL.                                                                                                                                                                                                         |
| `RECONCILE_ARTIFACTS`           | `false`                           | ні                           | Запустити одноразову звірку SQL та об'єктного сховища, вивести JSON і завершитися без запуску HTTP-серверів.                                                                                                                                                                                                                                                                 |
| `RECONCILE_REPAIR`              | `false`                           | ні                           | Разом зі звіркою видалити лише осиротілі об'єкти; записи про релізи з відсутніми або пошкодженими об'єктами не змінюються.                                                                                                                                                                                                                                                   |

Сховища артефактів:

- `ARTIFACT_BACKEND=gcs` використовує Google Cloud Storage;
- `ARTIFACT_ENDPOINT` за замовчуванням дорівнює `https://storage.googleapis.com`; якщо його перевизначити, GCS API та URL артефактів використовують цю кінцеву точку без зміни глобальних змінних середовища процесу;
- `ARTIFACT_BUCKET` задає контейнер для артефактів;
- `ARTIFACT_PROJECT` задає проєкт лише тоді, коли ввімкнене створення контейнера під час запуску;
- Для роботи з попередньо створеним контейнером GCS потрібні права створення, читання, видалення та переліку об'єктів, наприклад `roles/storage.objectAdmin` на цей контейнер. `ARTIFACT_ENSURE_BUCKET=true` додатково потребує `storage.buckets.get` і `storage.buckets.create`; для workload identity цей параметр варто лишати вимкненим, якщо сервіс не має створювати контейнер.
- `ARTIFACT_PREFIX` задає префікс сховища для модульних артефактів;
- `ARTIFACT_BACKEND=s3` використовує S3-сумісне об'єктне сховище, зокрема режим сумісності GCS, MinIO або Ceph; backend зобов'язаний атомарно виконувати `If-None-Match: *` для `PutObject`, бо від цього залежить незмінність create-only записів і це перевіряє integration test підтримуваного backend;
- для `s3` треба задати `ARTIFACT_ENDPOINT`, `ARTIFACT_BUCKET`, `ARTIFACT_ACCESS_KEY_ID`, `ARTIFACT_SECRET_ACCESS_KEY`; `ARTIFACT_PATH_STYLE=true` корисний для кінцевих точок GCS і MinIO зі шляховою адресацією;
- `UPSTREAM_SYNC_INTERVAL` вмикає фонове оновлення вже закешованих зовнішніх модулів з офіційного Forge;
- `UPSTREAM_SYNC_LIMIT` обмежує кількість зовнішніх модулів на один цикл оновлення.
- `UPSTREAM_SYNC_CONCURRENCY` обмежує кількість паралельних запитів одного циклу оновлення.

Сховище бази даних:

- `DATABASE_DSN=postgres://...` вмикає PostgreSQL;
- `DATABASE_DSN=sqlite:///data/puppet-forge.db` вмикає SQLite;
- тип бази даних визначається схемою DSN, тому окремий `DB_BACKEND` не потрібен;
- PostgreSQL призначений для промислового розгортання з кількома репліками. SQLite підходить для локального розгортання з одним процесом запису; оренда оновлення там спирається на блокування транзакцій SQLite, а не на PostgreSQL `FOR UPDATE NOWAIT`.

Керування доступом:

- конфігурація доступів зберігається тільки в БД: `access_teams`, `access_tokens`, `access_publish_owners`, `access_oidc_mappings`;
- `ACCESS_JSON` більше не використовується;
- `ADMIN_TOKEN` — початковий та аварійний токен середовища виконання, який не записується в БД;
- токени доступу генеруються як непрозорі облікові дані, показуються лише один раз і зберігаються як HMAC-SHA256-відбиток із видимим префіксом та метаданими життєвого циклу; кожен новий токен має обов'язкову зрозумілу назву та роль `read` або `publish`, а активна пара назви й ролі має бути унікальною в межах команди; `ACCESS_TOKEN_PEPPER` має бути однаковим на всіх репліках; кожен захищений bearer-запит перевіряє активність token ID у SQL, тому відкликання та завершення строку дії застосовуються на всіх репліках одразу після commit; сторінка керування має одну форму створення та незалежну пагінацію списку активних токенів і неактивної історії: початково по 20 записів із запам'ятовуваним вибором 10, 20, 50 або 100; `ACCESS_TOKEN_HISTORY_TTL` видаляє старі метадані прострочених і відкликаних токенів;
- якщо БД не містить жодних чинних облікових даних доступу і `ADMIN_TOKEN` не заданий, сервіс не стартує;
- під час чистого старту увійди в `/manage` через `ADMIN_TOKEN`, відкрий `/manage/teams` і налаштуй команди, токени, групи OIDC та адміністраторів команд OIDC; додаткові простори публікації призначають глобальні адміністратори у `/manage/admin/spaces`, а глобальні зіставлення OIDC налаштовуються у `/manage/admin/access`;
- кожне значення токена має бути глобально унікальним для всіх команд і ролей доступу; повторне використання токена читання, публікації або адміністратора відхиляється без виведення самого токена в помилці;
- `platform-admin` є зарезервованим ідентифікатором конфігурації для токенів глобального адміністратора та зіставлень OIDC; його не можна створити або використати як команду публікації чи простір модулів;
- вебінтерфейс (`/` і `/modules/...`) лишається відкритим для інформаційного перегляду локальних модулів незалежно від `PUBLIC_MODULE_ACCESS`;
- токени читання дають доступ до API читання, завантаження та `/v3/*`;
- токени публікації дають доступ до читання й публікації або оновлення лише в дозволених просторах, але не дають права видаляти модулі;
- глобальні адміністратори призначають і відкликають `Extra publish spaces` у `/manage/admin/spaces`; ці простори дозволяють публікацію та оновлення, але не надають права видалення; глобальні адміністратори можуть видаляти модулі та версії в будь-якому просторі імен, а адміністратори команд OIDC — лише в основному просторі своєї команди;
- зіставлення адміністраторів команд OIDC дають доступ до редагування токенів і груп OIDC тільки своєї команди;
- зіставлення глобальних адміністраторів OIDC дають повний доступ до адміністративних маршрутів (`/manage/admin/*`) і видалення модулів або версій у будь-якому просторі імен;
- `PUBLIC_MODULE_ACCESS=false` вимагає токен читання, публікації або адміністратора для маршрутів встановлення й API: читання API, завантаження релізів, видобутих файлів модулів і `/v3/*`;
- `PUBLIC_MODULE_ACCESS=true` відкриває ці маршрути без токена, щоб r10k і `puppet module install` могли забирати модулі без заголовка Authorization;
- `PUBLIC_MODULE_ACCESS=true` не відкриває публікацію, видалення, `/manage` або `/manage/admin/*`.

Вебавтентифікація:

- `WEB_AUTH_MODE=oidc` вмикає вхід із сесією для вебінтерфейсу через OIDC/Authentik;
- API при цьому й надалі використовує токени команд;
- потрібні `OIDC_ISSUER_URL`, `OIDC_CLIENT_ID`, `OIDC_CLIENT_SECRET`, `OIDC_COOKIE_SECRET`;
- стан авторизації одноразовий і живе п'ять хвилин; потік використовує прив'язку nonce, PKCE S256, строгі перевірки видавця, аудиторії, підпису й строку дії та список дозволених `OIDC_SIGNING_ALGORITHMS`;
- браузер отримує лише непрозорий ідентифікатор OIDC-сесії; атрибути ідентичності, прив'язаний до сесії секрет CSRF, строк дії, час останньої активності й стан відкликання зберігаються в SQL, а вихід відкликає спільний запис сесії;
- `OIDC_SCOPES` за замовчуванням дорівнює `openid profile email`; додай `groups`, якщо постачальник повертає твердження про групи лише з цією областю доступу;
- `OIDC_REDIRECT_URL` можна задати явно, але зазвичай краще лишити порожнім: сервіс побудує URL перенаправлення з імені вузла й протоколу поточного запиту та додасть `/auth/callback`;
- `PUBLIC_BASE_URL` не обов'язковий і використовується лише як резервне значення. За замовчуванням береться `Host` запиту, тому кілька імен Ingress працюють без канонічного URL;
- передані проксі ім'я вузла й протокол ігноруються, якщо одночасно не задані `TRUST_FORWARDED_HEADERS=true` та відповідний `TRUSTED_PROXY_CIDRS`; за завершення HTTPS на Ingress налаштуй обидва параметри, а `ALLOWED_PUBLIC_HOSTS` використовуй як список дозволених імен Ingress;
- у Kubernetes Ingress має передавати справжній `Host` і схему через `X-Forwarded-Host`/`X-Forwarded-Proto` або RFC `Forwarded`;
- для командного вебінтерфейсу користувачі OIDC зіставляються з командами через `oidc_groups`;
- для делегованого доступу адміністратора команди OIDC-користувачі зіставляються через `oidc_team_admin_emails` або `oidc_team_admin_groups`.

Вебінтерфейс команди:

- `/manage` відкриває огляд доступних модулів і команд без форм керування;
- `/manage/modules` відкриває загальну сторінку публікації, пошуку та видалення модулів;
- `/manage/teams` показує тільки доступні користувачу команди, а `/manage/teams/{team}/access`, `/tokens` і `/modules` розділяють налаштування доступу, токени та модулі вибраної команди;
- публічні й Manage-списки з пагінацією оновлюють через progressive HTML fragment-запити лише власні рядки, лічильники та елементи керування; фільтри й browser history працюють через той самий механізм, звичайна навігація лишається fallback, а розмір сторінки запам'ятовується з вибором 10, 20, 50 або 100 елементів;
- `/manage/admin/access` доступний глобальним адміністраторам для керування глобальними зіставленнями OIDC;
- `/manage/admin/spaces` доступний лише глобальним адміністраторам для призначення додаткових просторів публікації командам і відкликання цих призначень; основний простір із назвою команди створюється автоматично й не відкликається;
- адміністратори команд керують токенами та OIDC-відповідностями своєї команди на окремих сторінках `/manage/teams/{team}/tokens` і `/access`;
- адміністратори команд не можуть перейменовувати або видаляти команди, змінювати `Extra publish spaces`, редагувати глобальних адміністраторів чи змінювати чужі команди;
- головна сторінка керування має назву `Overview`, а глобальна навігація окремо містить `Modules` і `Teams`;
- якщо `WEB_AUTH_MODE=oidc`, `/manage` використовує вхід через OIDC і права з БД;
- без OIDC або як резервний варіант можна увійти через наявний токен із роллю `publish` із БД або через стартовий `ADMIN_TOKEN`;
- ідентичність OIDC або токен публікації бачить і змінює тільки простір із назвою `Team` та додаткові простори з `Extra publish spaces`;
- токен публікації не може видаляти модулі або версії;
- адміністратор команди через OIDC може видаляти модулі й версії тільки в основному просторі своєї команди;
- `ADMIN_TOKEN` або адміністративне зіставлення OIDC можуть видаляти модулі й версії в будь-якому просторі імен;
- версії, які завантажували r10k або `puppet module install` протягом `ACTIVE_RELEASE_TTL`, позначаються як `in use`, і кнопка видалення для них у `/manage/modules` не показується;
- `ACTIVE_RELEASE_TTL` за замовчуванням дорівнює `720h` / 30 днів і задається при старті сервісу;
- записи використання, старші за активне вікно, очищує щоденний worker під спільною SQL lease; перший асинхронний цикл запускається одразу після старту;
- перевірка політики та зміна SQL виконуються під одним module lock між репліками; транзакція видаляє metadata й додає кожен локальний object path у надійний `artifact_deletions` outbox. Singleton worker щохвилини повторює idempotent видалення зі сховища. Помилка сховища залишає лише object і завдання outbox, але не metadata, що посилаються на відсутній архів. Перед cleanup повторна публікація перевіряється під тим самим module lock;
- видалення метаданих індексованого зовнішнього релізу не додає спільний об'єкт із `upstream-cache/` до негайного видалення; щоденний worker, обраний через lease, прибирає лише об'єкти без SQL-посилань, старші за `UPSTREAM_ARTIFACT_ORPHAN_TTL`;
- видалені зовнішні версії записуються в таблицю позначок `deleted_releases`, тому фонова синхронізація не відновлює їх одразу; прямий запит саме цього релізу від r10k, Puppet або іншого клієнта навмисно відновлює його на вимогу, щоб старі середовища залишалися придатними до розгортання без фонового повернення всіх видалених версій; `DELETED_RELEASE_TTL` визначає строк фонового блокування, після якого реліз може повернути й синхронізація;
- видалення всього модуля очищає позначки видалення та дані використання релізів, тому після повторної синхронізації модуль можна створити заново з усіма доступними зовнішніми релізами;
- форма завантаження використовує ті самі правила, що й `POST /api/v1/modules`: користувач вибирає дозволений простір і файл, а ідентичність та метадані беруться з `metadata.json`.

Для `docker-compose.yml` OIDC можна ввімкнути через змінні середовища:

```bash
export WEB_AUTH_MODE=oidc
export PUBLIC_MODULE_ACCESS="false"
export OIDC_ISSUER_URL="https://auth.example.com/application/o/puppet-forge/"
export OIDC_CLIENT_ID="puppet-forge"
export OIDC_CLIENT_SECRET="replace-me"
export OIDC_COOKIE_SECRET="32-byte-random-secret"
# Необов'язково: якщо виявлення не повертає `end_session_endpoint`.
export OIDC_LOGOUT_URL="https://auth.example.com/application/o/puppet-forge/end-session/"
export ADMIN_TOKEN="replace-me-bootstrap-token"
export ACCESS_TOKEN_PEPPER="replace-with-at-least-32-random-bytes"
export MANAGE_SESSION_SECRET="replace-with-a-different-32-byte-secret"
docker compose up
```

У локальному [`docker-compose.yml`](docker-compose.yml) ці значення беруться зі змінних середовища або `.env`, а сервіс також підтримує відповідні аргументи командного рядка. Приклад без секретів міститься в [`.env.example`](.env.example). За замовчуванням Compose стартує з `WEB_AUTH_MODE=none`, щоб випадково не зберігати секрети OIDC у YAML.

Для локального Authentik часто краще не використовувати `localhost` в URI перенаправлення. Задай локальне DNS-ім'я, яке відкривається у браузері, наприклад:

```bash
open "http://forge.127.0.0.1.nip.io:8080"
```

У постачальнику Authentik треба додати точний URI перенаправлення:

```text
http://forge.127.0.0.1.nip.io:8080/auth/callback
```

Якщо сервіс доступний через кілька імен Ingress і `OIDC_REDIRECT_URL` порожній, треба додати адресу зворотного виклику для кожного імені, наприклад:

```text
https://forge.example.com/auth/callback
https://forge.dev.example.com/auth/callback
```

Якщо Authentik запущений у Docker або на основній машині, `OIDC_ISSUER_URL` також має бути доступний із контейнера `app`. Для Authentik на основній машині з Docker Desktop це зазвичай ім'я `host.docker.internal`, але URI перенаправлення все одно має бути URL сервісу Forge, який відкриває браузер.

Вихід із `/manage` відкликає збережену в SQL сесію токена або OIDC та очищає локальні файли cookie. Якщо виявлення OIDC повертає `end_session_endpoint` або задано `OIDC_LOGOUT_URL`, сервіс також спрямовує браузер на вихід у постачальника, щоб Authentik не виконав автоматичний повторний вхід зі старої сесії.

Обмеження частоти входу й публікації зберігаються в обмеженій пам'яті окремо в кожній репліці. Завершені лічильники клієнтів видаляються за допомогою черги пріоритетів; при досягненні межі витісняється лічильник із найближчим часом скидання, щоб нові клієнти не втрачали доступ до сервісу. `TRUSTED_PROXY_CIDRS` дає змогу розрізняти клієнтів за довіреним Ingress; залишай параметр порожнім, якщо CIDR проксі невідомі. Для строгого спільного або стійкого до зловмисної ротації адрес обмеження додатково налаштуй у спільному обмежувачі на Ingress або шлюзі API.

Для групи користувачів краще зіставляти не адреси електронної пошти, а групу OIDC:

```json
{
  "team": "teamname",
  "oidc_groups": ["teamname-devops"]
}
```

В Authentik треба додати користувачів до групи `teamname-devops` і переконатися, що застосунок або постачальник повертає атрибут `groups` в ID-токені. Для командного доступу у вебінтерфейсі використовується саме `oidc_groups`: одна група OIDC дає команді доступ до публікації в просторі з назвою `Team`. Якщо треба дозволити інші простори, глобальний адміністратор призначає їх у `/manage/admin/spaces`. Якщо після входу зіставлення не спрацює, сервіс запише `email`, `subject`, `domain` і `groups` у повідомленні `oidc session is not mapped to team`.

Щоб команда сама керувала своїми токенами й групами OIDC, додай адміністраторів команди в полі `OIDC team admins`. Для однієї-двох людей зручніше використовувати адреси електронної пошти; коли список зростає, краще перейти на групу:

```json
{
  "team": "teamname",
  "oidc_groups": ["teamname-devops"],
  "oidc_team_admin_emails": ["owner@example.com"],
  "oidc_team_admin_groups": ["teamname-admins"]
}
```

Користувач з адресою `owner@example.com` або з групи `teamname-admins` може відкрити `/manage/teams` та окремі сторінки Access, Tokens і Modules для `teamname`. Він не зможе змінити інші команди, глобальних адміністраторів, конфігурацію JSON або додаткові простори публікації. Такий адміністратор може видаляти модулі й версії лише в основному просторі своєї команди. `Extra publish spaces` дають право публікації та оновлення, але видаляти в них може лише глобальний адміністратор.

Одну адресу або групу адміністратора команди OIDC можна додати до кількох команд. Тоді користувач побачить і зможе редагувати всі ці команди в `/manage/teams`, але не матиме доступу до чужих команд або налаштувань глобальних адміністраторів.

Адміністративний OIDC-доступ задається у блоці `Global OIDC Admins`, щоб не змішувати командні групи публікації з глобальними правами:

```json
{
  "team": "platform-admin",
  "oidc_admin_groups": ["forge-admins"]
}
```

Користувач із групи `forge-admins` може відкривати `/manage/admin/access` і видаляти модулі й версії в будь-якому просторі імен. Публікація через адміністративну групу автоматично не вмикається.

Якщо користувач OIDC одночасно входить до командної групи, наприклад `teamname-devops`, і має адміністративне зіставлення через `oidc_admin_groups`, `oidc_admin_emails` або `oidc_admin_subjects`, сервіс об'єднує обидва набори прав. Доступ глобального адміністратора не прибирає командну публікацію або керування командою.

Щоб додати себе як адміністратора після першого запуску:

1. Увійди в `/manage` через `ADMIN_TOKEN`.
2. Відкрий `/manage/admin/access`.
3. У `Global OIDC Admins` додай свою адресу електронної пошти в `OIDC admin emails` або свою групу в `OIDC admin groups`.
4. Збережи глобальних адміністраторів і перелогінься через OIDC.

`ADMIN_TOKEN` варто залишати тільки для початкового або аварійного доступу, наприклад коли OIDC не працює. Для щоденного доступу використовуй групи або адреси електронної пошти адміністраторів OIDC у полях `OIDC admin groups` чи `OIDC admin emails`.

Створюй і редагуй команди на сторінці `/manage/teams`, призначай додаткові простори публікації на сторінці `/manage/admin/spaces`, а глобальних адміністраторів OIDC налаштовуй на сторінці `/manage/admin/access`. Сервіс навмисно не має endpoint для повної заміни конфігурації у форматі JSON. Токени доступу створюй, ротуй і відкликай на сторінці `/manage/teams/{team}/tokens`; початкове значення нового токена показується лише один раз.

Під час оновлення бази зі старою колонкою відкритого тексту `access_tokens.token` сервіс при старті переносить облікові дані до HMAC-схеми. Задай і збережи `ACCESS_TOKEN_PEPPER` до першого запуску оновленої версії: наявні початкові токени продовжать працювати, але втрата або зміна цього секрету зробить їх недійсними.

Helm-чарт також підтримує ресурси Prometheus Operator, але за замовчуванням вони вимкнені:

- `serviceMonitor.enabled=true` створює `ServiceMonitor`, який вибирає внутрішній сервіс лише для метрик;
- `prometheusRule.enabled=true` створює `PrometheusRule` із тими самими правилами запису й оповіщення, що й [examples/prometheus/alerts/puppet-forge.yml](examples/prometheus/alerts/puppet-forge.yml). Робочі пороги можна налаштувати в `prometheusRule.thresholds`; типові значення відповідають окремому прикладу.

## Docker Compose

Для локального стенду:

```bash
docker compose up --build
```

Сервіс буде доступний на `http://localhost:8080`.

Compose запускає:

- `app` з цим сервісом;
- `postgres`; сервіс сам створює потрібні таблиці при старті;
- `fake-gcs-server` як локальний емулятор GCS;
- `minio` і `minio-init` для локального S3-сумісного сховища;
- `r10k` контейнер на базі `puppet/r10k`, який автоматично запускає `puppetfile install` і завершує роботу.

Локальне сховище можна перемикати так:

- `ARTIFACT_BACKEND=gcs` використовує `fake-gcs-server` через `ARTIFACT_ENDPOINT=http://gcs:4443`
- `ARTIFACT_BACKEND=s3` використовує `MinIO` через `ARTIFACT_ENDPOINT=http://minio:9000`

Окрема перевірка `r10k` через Compose:

1. Підніми стенд:

    ```bash
    docker compose up --build
    ```

2. Переконайся, що потрібні модулі доступні локально. Для зовнішніх модулів запит r10k на маршрут `/v3/*` сам проіндексує метадані й закешує артефакт.

3. У `testdata/r10k/Puppetfile` тримай лише зовнішні модулі для димової перевірки. Локальні приватні модулі треба спершу опублікувати у Forge, інакше r10k очікувано отримає `module does not exist`.

4. Запусти одноразовий контейнер `r10k`:

    ```bash
    docker compose up r10k
    ```

Після завершення:

```bash
docker compose logs r10k
```

Базова конфігурація в `docker-compose.yml` звертається до локального Forge за адресою `http://app`, тобто за іменем сервісу контейнера `app` у мережі Docker Compose, і передає локальний `ADMIN_TOKEN` як `authorization_token`, щоб димова перевірка працювала у приватному режимі `PUBLIC_MODULE_ACCESS=false`. Одноразовий контейнер установлює модулі в `/tmp/r10k-modules`, тому перевірка не залежить від прав на основній машині або іменованого тому.

## Makefile

Для стандартних локальних команд є [`Makefile`](Makefile):

```bash
make help
make check
make test-browser
make test-postgres
make test-object-storage
make compose-up
make http-smoke
make docker-smoke
make compose-smoke
make r10k
make oidc-preflight
make helm-package
```

`make http-smoke` перевіряє вже запущений HTTP-сервіс: `/healthz`, `/readyz`, окремий сервер метрик, публічний HTML-каталог, форму входу за токеном, політику API читання для `PUBLIC_MODULE_ACCESS` та читання і вхід за адміністративним токеном. Для нестандартних URL або токена використовуй `SMOKE_BASE_URL`, `SMOKE_METRICS_URL`, `SMOKE_PUBLIC_MODULE_ACCESS` і `SMOKE_ADMIN_TOKEN`. `make docker-smoke` збирає фінальний image і перевіряє non-root UID, вбудовану версію, довідку та OCI-мітку версії. `make compose-smoke` запускає локальний набір Compose у фоновому режимі, виконує димові перевірки HTTP та одноразовий запуск r10k, а код завершення r10k повертає до Makefile.

За замовчуванням Makefile використовує `go`, `gofmt` і `golangci-lint` з `PATH`. Інструменти можна перевизначати через змінні:

```bash
GO=go GOLANGCI_LINT=golangci-lint make check
```

Перед `make test-browser` один раз виконай `npm ci`. У CI браузерні тести
виконуються у зафіксованому Playwright-контейнері, тому версії браузера й test runner
залишаються узгодженими.

## Kubernetes

Helm-чарт міститься в [`deploy/puppet-forge`](deploy/puppet-forge).

Приклад:

```bash
helm upgrade --install puppet-forge ./deploy/puppet-forge \
  --set secret.existingSecret=puppet-forge-runtime
```

Чарт вимагає рівно один режим секретів: або задай `secret.existingSecret`, або явно
встанови `secret.create=true` і передай обов'язкові значення. За замовчуванням чарт
не створює облікових даних. Створи вказаний Secret через менеджера секретів або
контролер GitOps щонайменше з `DATABASE_DSN`, `ACCESS_TOKEN_PEPPER` та окремим
`MANAGE_SESSION_SECRET`; додай облікові дані сховища артефактів та OIDC, потрібні
конкретній конфігурації. Для ізольованої локальної перевірки чарту використовуй
`-f deploy/puppet-forge/values-dev.yaml`.

Коли зовнішній менеджер оновлює цей Secret без зміни Deployment manifest, задай у `secret.rolloutChecksum` версію або digest зовнішнього Secret. Зміна значення оновить pod template і запустить контрольований rollout.

За замовчуванням чарт використовує фіксований `replicaCount`. Щоб кількістю реплік керував Kubernetes, увімкни HPA:

```bash
helm upgrade --install puppet-forge ./deploy/puppet-forge \
  --set autoscaling.enabled=true \
  --set autoscaling.minReplicas=2 \
  --set autoscaling.maxReplicas=6
```

Коли `autoscaling.enabled=true`, Deployment не створює `spec.replicas`; цим керує `HorizontalPodAutoscaler`. Не вмикай автоматичне масштабування з `sqlite://` у `DATABASE_DSN`; для кількох реплік потрібні PostgreSQL та однакові `ACCESS_TOKEN_PEPPER` і `MANAGE_SESSION_SECRET` на всіх репліках.

Чарт запускає контейнер без прав суперкористувача, із кореневою файловою системою лише для читання, профілем seccomp `RuntimeDefault`, без можливостей Linux і з `emptyDir` у `/tmp`. Монтування токена службового облікового запису за замовчуванням вимкнене. PDB та розподіл за топологією ввімкнені; `networkPolicy.enabled` лишається вимкненим, доки явно не задано дозволених вузлів через `networkPolicy.ingress` і `networkPolicy.egress`.

Ресурси Prometheus Operator вимкнені за замовчуванням. Для збирання метрик і оповіщень увімкни `serviceMonitor.enabled=true` та `prometheusRule.enabled=true`.

## GitHub Actions

Процес [`CI`](.github/workflows/ci.yml) запускається для запитів на злиття та надсилання змін до будь-якої гілки. Він викликає повторно використовуваний процес [`Checks`](.github/workflows/checks.yml), який перевіряє форматування, `go vet`, `golangci-lint`, поріг покриття, browser-регресії Playwright, сумісність із PostgreSQL, інтеграцію з об'єктним сховищем, конфігурацію Docker Compose, перевірку й пакування Helm, окремі та згенеровані Helm-правила Prometheus, димову перевірку архівів релізу, пробну збірку Docker-образу, тести перегонів даних, `govulncheck`, `gosec` і сканування файлової системи та образу через Trivy.

Процес [`Release`](.github/workflows/release.yml) запускається тільки для тегів `v*.*.*`. Перед публікацією він викликає той самий повторно використовуваний процес перевірок, але без локальної перевірки конфігурації Docker Compose, а для тегу `v1.2.3` будує:

- файли GitHub Release з бінарними архівами в `dist/*.tar.gz`, `dist/checksums.txt` і CycloneDX SBOM у `dist/sbom.cdx.json`;
- підписаний багатоплатформний Docker-образ `ghcr.io/<owner>/<repo>:v1.2.3` з BuildKit SBOM/provenance attestations; `:latest` просувається з цієї версії лише після успішної публікації бінарного релізу, версійного образу та Helm chart;
- Helm-чарт `puppet-forge-1.2.3.tgz` із `appVersion: v1.2.3`;
- індекс репозиторію Helm у GitHub Pages через `helm/chart-releaser-action`. Чарт публікується через репозиторій Helm, а не як файл GitHub Release.

Процес спочатку створює чернетку GitHub Release, потім публікує та підписує незмінну версію Docker image, а після неї Helm chart. Лише коли всі версійні компоненти успішні, він оновлює метадані вихідного Helm-чарту, просуває `:latest` і робить GitHub Release публічним. Додатковий metadata-коміт містить `[skip ci]`, щоб звичайний CI гілки не запускався лише через оновлення метаданих релізу.

Для репозиторію Helm потрібна гілка `gh-pages` і GitHub Pages, налаштований на цю гілку. Після релізу репозиторій Helm-чарту можна додати так:

```bash
helm repo add puppet-forge https://<owner>.github.io/<repo>
helm repo update
helm upgrade --install puppet-forge puppet-forge/puppet-forge
```

## Додаткова документація

- Схеми роботи одного та кількох екземплярів: [SCHEMA.uk.md](SCHEMA.uk.md)
- Метрики: [METRICS.uk.md](METRICS.uk.md)
- Архітектура: [ARCHITECTURE.uk.md](ARCHITECTURE.uk.md)
- Резервне копіювання та відновлення: [BACKUP_RESTORE.uk.md](BACKUP_RESTORE.uk.md)
- Grafana dashboard: [examples/grafana/puppet-forge-dashboard.json](examples/grafana/puppet-forge-dashboard.json)
- Prometheus rules: [examples/prometheus/alerts/puppet-forge.yml](examples/prometheus/alerts/puppet-forge.yml)
